# Rate limiting zones
limit_req_zone $binary_remote_addr zone=run:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=share:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=snippet:10m rate=30r/m;

server {
    listen 80;
    server_name api.sans.dev;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name api.sans.dev;

    ssl_certificate /etc/letsencrypt/live/api.sans.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.sans.dev/privkey.pem;

    client_max_body_size 1m;

    location /api/run {
        limit_req zone=run burst=3 nodelay;
        proxy_pass http://127.0.0.1:8090;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 15s;
    }

    location /api/share {
        limit_req zone=share burst=3 nodelay;
        proxy_pass http://127.0.0.1:8090;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/snippet/ {
        limit_req zone=snippet burst=5 nodelay;
        proxy_pass http://127.0.0.1:8090;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/health {
        proxy_pass http://127.0.0.1:8090;
    }
}
